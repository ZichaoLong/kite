# AGENTS.zh-CN.md

本文件仅记录仓库所有者的设计与开发偏好。

不要将本文件视为仓库架构、模块边界或功能语义的事实来源。按需阅读 `docs/` 下的相关文档。

本文件移植自 FOCUS 的同名文件，工程立场完全一致；KITE 特有约定见末节。

## 核心偏好

默认倾向于：

- 清晰的架构
- 易于维护
- 行为明确无歧义

不要默认倾向于：

- 仅为了兼容性而保留兼容
- 因为已经存在就继续保留薄弱抽象
- 直接把模糊的产品行为编码进实现

## 默认工程立场

进行修改时：

- 优先显式契约，而不是隐式约定
- 优先一条清晰路径，而不是多条半支持路径
- 优先消除歧义，而不是保留历史形状
- 优先简单控制流，而不是"巧妙"分层
- 优先 fail-closed 行为，而不是含糊的 best-effort 行为

如果某个功能契约不清晰，就把歧义暴露出来，并通过代码、命名、校验或文档把契约收紧。

## 兼容性

兼容性不是这个仓库的默认目标。

除非用户明确提出相反要求：

- internal API 可以自由调整
- 过时分支和兼容垫片可以移除
- 如果结果更清晰、更容易推理，行为可以被简化

例外：对上游 kimi-code 的 kap-server API 依赖属于**外部**契约，不适用本条——上游演进不受本仓库控制，须以 CI 快照 diff 感知漂移、在适配层内显式适配（版本策略见 `docs/architecture/kite-design.md` §10：跟随，不钉死）。

## 重构倾向

只要能提升清晰度，就鼓励重构。

好的重构通常会：

- 让所有权更清晰
- 减少隐藏耦合
- 删除重复路径
- 减少运行时状态或行为上的歧义

坏的重构通常会：

- 只是搬运复杂度，而没有澄清所有权
- 增加抽象，却没有简化代码
- 仅为了避免改动而保留令人困惑的结构

## Review 优先级

按以下顺序优先关注：

1. 有歧义或错误的行为
2. 状态、事件或职责所有权不清晰
3. 模块之间的隐藏耦合
4. 并发或生命周期风险
5. 高风险流程缺少回归覆盖
6. 掩盖意图的命名或结构

## 测试偏好

不要停留在"测试通过"。

在可行时，添加或更新测试来固化这次变更的目标行为，尤其是 bug、状态迁移、所有权转移以及其他高风险流程。

测试从第一天起进 CI（FOCUS 缺测试 CI 的教训不重复）。

## 文档策略

不要把仓库事实放进这个文件里。

- 架构、边界和运行时设计应写在专门文档中。
- 功能契约和行为语义应写在专门文档中。
- 当为具体场景新增或修改重要功能、命令、概念或抽象时，优先把它的设计意图记录到 `docs/` 下相关文档中，而不是只记录表面行为。
- 在可行时，优先把以下三点写清楚：
  - 它要解决什么问题或场景
  - 它作用于哪一层状态或抽象边界
  - 为什么现有机制不够用
- 这样做主要是为了保留"它为什么存在"，以便后续重构时仍然能判断它应该被保留、拆分、简化还是删除。
- 只有在任务需要时才阅读这些文档。
- 技术文档为中英双语：英文为规范版（`<name>.md`），简体中文为配对译本（`<name>.zh-CN.md`）；配对关系由 CI 中的 `scripts/check-docs.sh` 强制。

**功能承载力门槛**：任何功能进入开发前，必须先在其合同文档中回答四个问题——归哪一层？动哪条状态轴？崩溃/重启后怎么恢复？用什么测试锁住行为？答不出来就砍需求，不加例外。

## 克制原则

功能增速不得超过架构承载力。宁可少做、做透，不做多、做漏。

- 新功能默认拒绝，除非通过承载力门槛
- 每个功能必须有明确的失败模式（fail-closed），不允许"出错时尽力而为"
- Non-goals 与 goals 同等重要，写进合同文档

## KITE 特有约定

- **上游 source of truth**：kap-server 行为以 `~/llm/kimi/kimi-code` 的实际代码为准；二手描述不可信时先读上游代码。
- **架构蓝本**：FOCUS（`~/llm/focus`）。复用其上游无关资产（飞书传输层、卡片、RuntimeLoop、stores、service 管理）时，按 KITE 词汇（session/agent/prompt/approval/question）重命名，不保留 codex 术语。
- **上游版本跟随，不钉死**（2026-07-21 对齐）：不指望长期停留在一个旧 kimi-code 版本上；CI 必须有 kap-server OpenAPI 快照 diff 护栏感知漂移；安装/启动做版本检测，与已验证版本不符时警告但不阻止运行。
- **安装纪律**（沿用 FOCUS）：不使用 `pip install .` / `pip install -e .`；唯一支持的安装路径是仓库提供的 install 脚本。
