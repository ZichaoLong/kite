name: Docs

on:
  pull_request:
  push:
    branches:
      - master

jobs:
  docs-check:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run doc checks
        run: bash scripts/check-docs.sh
